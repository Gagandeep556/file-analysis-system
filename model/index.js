const db = require("./db");

const FileInfo = require("./fileInfo.model");

const Result = require("./result.model");

const Task = require("./task.model");

module.exports = {
  db,
  Result,
  FileInfo,
  Task,
};
